#!/bin/bash
# update package

# install git
# install zsh
# install vim
# install tmux
# install gdb

# copy .gitconfig to ~
# copy .gitignore to ~

# install oh-my-zsh
# install lambda-zsh-theme
# install zsh-autosuggestion
# (DON'T NEED TO INSTALL bulit-in) install zsh-z
# copy .zsh_aliases to ~
# chsh to zsh

# install vim-Vundle
# copy .vimrc to ~
# install vim-plugin

# copy .tmux.conf to ~

# install gdb-gef
# copy .gdbinit to ~

# install exa
# install bat
# install fd
