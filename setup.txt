#!/bin/bash
# update package

# install git
# copy .gitconfig to ~
# copy .gitignore to ~

# install zsh
# install oh-my-zsh
# install lambda-zsh-theme
# install zsh-autosuggestion
# install zsh-z
# copy .zsh_aliases to ~
# chsh to zsh

# install vim
# install vim-Vundle
# copy .vimrc to ~
# install vim-plugin

# install tmux
# copy .tmux.conf to ~

# install gdb
# install gdb-gef
# copy .gdbinit to ~

# install exa
# install bat
# install fd
